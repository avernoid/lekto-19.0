<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="report_work_order_action" model="ir.actions.report">
        <field name="name">Work Order</field>
        <field name="model">stock.picking</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">custom_work_order.report_work_order</field>
        <field name="report_file">custom_work_order.report_work_order</field>
        <field name="print_report_name">'Orden de Trabajo - %s' % object.name</field>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_type">report</field>
        <field name="paperformat_id"
               ref="sale_corporate_quotation.paperformat_corporate_quotation"/>
    </record>

</odoo>
