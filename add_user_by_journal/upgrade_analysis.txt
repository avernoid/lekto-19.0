--- Migration Analysis: add_user_by_journal (v18 to v19) ---

1. Manifest Update:
   - Version updated from 18.0.1.0.0 to 19.0.1.0.0.
   - Core dependency 'account' verified for v19.

2. Model Analysis (account.payment.register):
   - Method '_get_batch_available_journals' refactored to use recordset filtering (res.filtered).
   - Logic remains consistent: restrict journals based on 'assign_to_ids' for non-admin users.

3. Model Analysis (account.move):
   - Field 'journal_assign_to_ids' and its compute method confirmed for v19.
   - Direct SQL query for 'journal_id' maintained to prevent recursion during batch processing or draft creation.

4. Model Analysis (ir.rule):
   - '_compute_domain' override verified against base/models/ir_rule.py in v19.
   - Cache key and signature remain stable.

5. View Analysis (account.journal):
   - XPath anchor updated: 'group_alias_edit' (v18) -> 'group_email_alias' (v19).
   - This ensures the 'assign' group appears correctly in the Advanced Settings tab.

--- Index Optimization ---
9. LLM & SEO Enhancement (static/description/index.html):
   - Integrated a structured metadata block for AI models and search engine indexing.
   - Enhanced visual hierarchy with professional typography and shadow-sm elements.
   - Highlighted Multi-Language Support (English/Spanish) with flag visuals.
   - Redesigned WhatsApp Call-to-Action for maximum conversion prominence.
   - Verified that all external links include 'target="_blank"'.
   - Optimized professional copy for and LLM-ready technical summary.


--- Test Suite Update ---
8. Modern Tests (tests/test_add_user_by_jornal.py):
   - Completely replaced the obsolete test file with a modern Odoo 19 implementation.
   - New tests cover: Journal visibility, Account Move visibility (Record Rules), and Administrator bypass.
   - Added '@tagged' decorator for professional test execution.

--- Interoperability & Security Overhaul (v19.0.1.0.37) ---
10. Major Architectural Improvements:
   - **Hybrid Access**: `ir_rule` updated to support `journal_group_ids` (M2M) alongside `assign_to_ids`. Logic is UNION (OR).
   - **Line-Level Security**: Added specific `ir_rule` for `account.move.line` to prevent `level_name` leaks in list views. Mirrors `account.move` logic.
   - **Interoperability Whitelist**: 
     - Modified `ir_rule` to allow access to restricted `account.move` (type='entry') if they are affecting AR/AP (Commercial Data), Payments, or Bank Statements.
     - **CRITICAL**: Whitelist strictly EXCLUDES `out_invoice`/`in_invoice` to avoid leaking primary documents.
   - **Test Coverage**: Added robust tests for Invoice Leaks, Payment Visibility, Group Access, and Mixed Access.