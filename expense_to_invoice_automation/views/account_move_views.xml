<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_inherit_expense_to_invoice" model="ir.ui.view">
        <field name="name">account.move.form.inherit.expense.to.invoice</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_expense_vendor_bills"
                        class="oe_stat_button"
                        icon="fa-bars"
                        type="object"
                        invisible="expense_vendor_bill_count == 0">
                    <field string="Vendor Bills" name="expense_vendor_bill_count" widget="statinfo"/>
                </button>
            </xpath>
        </field>
    </record>
</odoo>
