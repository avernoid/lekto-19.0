<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <!-- Each user only sees the rows they generated, within their allowed companies (§8). -->
    <record id="stock_kardex_valued_rule_own" model="ir.rule">
        <field name="name">Stock Kardex Valued: own rows / company</field>
        <field name="model_id" ref="model_stock_kardex_valued"/>
        <field name="domain_force">['&amp;', ('create_uid', '=', user.id), ('company_id', 'in', company_ids)]</field>
        <field name="groups" eval="[(4, ref('stock.group_stock_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

</odoo>
